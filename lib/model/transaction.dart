class Transaction {
  String title;
  String description;
  int amount;
  String type;
  Transaction(
      {required this.title,
      required this.description,
      required this.amount,
      required this.type});
}
